/*
package com.smoothstack.alinefinancial.Processors;

import com.smoothstack.alinefinancial.Caches.MerchantCache;
import com.smoothstack.alinefinancial.Models.Merchant;
import com.smoothstack.alinefinancial.Models.State;
import com.smoothstack.alinefinancial.Models.Transaction;
import org.springframework.batch.core.listener.ItemListenerSupport;
import org.springframework.batch.item.ItemProcessor;

public class MerchantProcessor extends ItemListenerSupport<Transaction, Merchant> implements ItemProcessor<Transaction, Merchant> {

    private final MerchantCache merchantCache = new MerchantCache();

    @Override
    public Merchant process(Transaction item) throws Exception {

    }
}
*/
